/// The service responsible for chatting with the AI.
pub mod chat_service;

/// The service responsible for receiving events and storing them in a queue.
pub mod event_service;

/// The service responsible for rendering the UI and managing the AppState.
pub mod ui_service;

/// Widgets contains my chat widget
pub mod widget;
